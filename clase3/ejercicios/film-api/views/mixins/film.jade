mixin film(film, button)
  .material-card
    img.card-img-top(src=film.image, alt='Card image cap')
    .card-body
      h5.card-title #{film.title}
      p.card-text
        | #{film.description}
      if button
        a.btn.btn-primary(href='/film/' + film.id) Ver más